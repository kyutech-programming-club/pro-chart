<%= @lang.name %>

<% if current_user %>
  <%= link_to "追加", new_lang_elem_path(@lang, @elems) %>
<% end %>

<% @elems.each do |elem| %>
   <br>
  <%= link_to elem.name, lang_elem_path(@lang, elem) %>
<% end %>



